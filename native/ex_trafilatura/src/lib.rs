//! The ExTrafilatura NIF crate.
//!
//! The NIF surface itself is not here yet — this crate currently exists to pin
//! the vendored, patched `trafilatura` through `[patch.crates-io]` and to host
//! the regression test for the patches we carry. See
//! `vendor/trafilatura/VENDOR.md` for what is vendored and why.
